# Skills Directory

这个目录预留给后续新增的 skill。

建议每个 skill 使用一个独立子目录，例如：

```text
skills/
  code_review/
    skill.md
  file_ops/
    skill.md
```

如果你愿意，下一步我可以继续帮你设计一套这个项目自己的 skill 格式，例如：

- `skill.md` 描述用途、触发条件、输入输出
- `config.json` 定义参数
- `examples/` 放示例
